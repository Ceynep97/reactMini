import React from "react";
import styled from "styled-components";

export default function OurWorks() {
  const Section = styled.div({
    height: " 100vh",
    scrollSnapAlign: "center",
  });
  return <Section>OurWorks</Section>;
}
